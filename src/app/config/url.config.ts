export enum AuthUrl {
  login = "/auth/login",
  register = "/auth/register",
}

export enum UserUrl {
  userInfo = "/user"
}

export enum GeolocationUrl {
  getCountries = "/core/geolocation/countries"
}

export enum MainUrl {
  home = "/",
  games = "/games",
  esports = "/esports",
}

export enum ProfileUrl {
  profile = "/profile"
}
